### 1
```sql

SELECT
	ID_Pessoa,
	Nome ,
	IIF(1 = 1, 'É ATIVO', 'É INATIVO') AS StatusAtivo
FROM
	PESSOAS	
```

### 2
```sql

SELECT  
    ID_Pessoa,
    CPF,
    IIF(NULLIF(CPF, '') IS NULL, 0, 1) AS TemCPF
FROM
    PESSOAS;
```
